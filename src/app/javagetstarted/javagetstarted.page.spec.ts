import { ComponentFixture, TestBed } from '@angular/core/testing';
import { JavagetstartedPage } from './javagetstarted.page';

describe('JavagetstartedPage', () => {
  let component: JavagetstartedPage;
  let fixture: ComponentFixture<JavagetstartedPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(JavagetstartedPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
