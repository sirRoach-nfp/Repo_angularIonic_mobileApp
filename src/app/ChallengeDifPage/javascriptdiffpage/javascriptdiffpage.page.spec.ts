import { ComponentFixture, TestBed } from '@angular/core/testing';
import { JavascriptdiffpagePage } from './javascriptdiffpage.page';

describe('JavascriptdiffpagePage', () => {
  let component: JavascriptdiffpagePage;
  let fixture: ComponentFixture<JavascriptdiffpagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(JavascriptdiffpagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
