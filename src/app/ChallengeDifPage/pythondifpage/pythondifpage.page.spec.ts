import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PythondifpagePage } from './pythondifpage.page';

describe('PythondifpagePage', () => {
  let component: PythondifpagePage;
  let fixture: ComponentFixture<PythondifpagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PythondifpagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
