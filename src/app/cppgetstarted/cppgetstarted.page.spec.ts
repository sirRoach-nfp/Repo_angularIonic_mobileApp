import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CppgetstartedPage } from './cppgetstarted.page';

describe('CppgetstartedPage', () => {
  let component: CppgetstartedPage;
  let fixture: ComponentFixture<CppgetstartedPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CppgetstartedPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
