import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PythondiffpagePage } from './pythondiffpage.page';

describe('PythondiffpagePage', () => {
  let component: PythondiffpagePage;
  let fixture: ComponentFixture<PythondiffpagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PythondiffpagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
